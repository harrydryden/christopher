# Shared
DATABASE_URL=postgres://postgres@127.0.0.1:5433/christopher_dev

# Web (Vercel)
SESSION_SECRET=change-me-to-a-long-random-string
# Generate with: pnpm --filter @christopher/web hash-password 'your password'
APP_PASSWORD_HASH=

# Worker (Render)
ANTHROPIC_API_KEY=
TZ=Europe/London
SCRAPER_CONTACT_EMAIL=you@example.com
# Optional: path to a Chromium binary if not using the Playwright Docker image
# CHROMIUM_EXECUTABLE_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome
