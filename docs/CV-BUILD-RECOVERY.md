# CV build recovery and progress — 13 September 2026

The repeated three-page error had a production cause: the Render worker was still running `e145570` from 11 September. The web application had the newer PDF renderer and download constraints, while the worker still generated content without the measured fitting or assessment pipeline. Render reported auto-deploy enabled, but its live release and recent deployment history showed none of the later CV changes. Deploying merged commit `da6aa3d` restored the fitting and assessment pipeline in production.

There were also two recovery gaps. Saving an oversized edit failed before it could reach the fitter. Assessment-only jobs accepted oversized legacy content and scored it without fitting. Saving now queues the wording; the worker measures it, fits overflow using the saved appearance, and only assesses the resulting PDF text. A final measured page-count assertion prevents any new ready draft exceeding two pages. Valid two-page edits keep the existing assessment-only path. Employment and qualifications remain protected; impossible layouts still fail without clipping or reducing font sizes.

The draft page now puts appearance, fitting, improvement and saving controls first. The company's exact saved description appears beside the editor with independent scrolling, minimise and hide/reopen controls. Closing the panel preserves unsaved state. On narrow screens the description stacks below the editor. PDF previews remain available for oversized legacy drafts and recoverable assessment failures; final downloads and application records retain their finalisation checks.

Build stages are stored with the draft: analysing, writing, fitting and assessing. Polling notices stage changes, not just queued/ready transitions. Its initial version comes from the rendered page, fixing a race where changes before the first poll could otherwise be missed. The wheel animation accompanies the actual stage; there are no timer-based percentage estimates. Reduced-motion mode uses the monochrome mark. A single additive migration, `0016_cv_build_progress.sql`, adds the nullable stage field. Retries clear stale stages and ready drafts clear progress.

Verification covers saved oversized content, preservation of edited appearance, scoring only fitted wording, stage reporting, full PDF rendering, and existing finalisation/application safeguards. The production web smoke check now exercises a real browser against synthetic drafts: appearance settings, hide/reopen and minimise controls, preservation and submission of edits, mobile overflow, reduced motion, queued revision creation, and polling through assessment to completion. No model calls are made by that browser check.

## Release verification

A merge alone does not prove either service has updated. For CV releases, verify the Vercel web deployment and the Render worker deployment against the intended commit; trigger the worker deployment if it is behind. The worker now includes its Render commit in its health response and heartbeat, and the Health page displays it. This makes the running release inspectable rather than inferred from GitHub or the auto-deploy setting. The main-branch CI release check now waits for that exact commit at the worker health endpoint and fails if the deployment stays behind; no deployment credentials are needed for this check. Check migration/start-up logs after deployment and inspect a newly generated or refitted draft, including its assessment and measured page count. Existing submitted application PDF bytes must remain unchanged.

## Live legacy-library follow-up

The first live refit uncovered a second compatibility issue: legacy prose skill blocks were allocated a non-zero structured skill count. The author returned `skillItems`, which the strict evidence materialiser correctly rejected because the source did not contain an explicit skill list. Legacy skill budgets now allocate zero structured items and the prompt directs their concise labels into bullets (which still render as pills). If the model uses the wrong representation, the existing bounded authoring loop supplies a precise correction and retries without relaxing source validation. Synthetic regression coverage reproduces the live failure and checks the corrected representation.

## Live assessment follow-up

The next production refit produced a verified two-page A4 PDF, including centred industry and skill pills, Skills before Education within the shared parent section, and 1 / 2 and 2 / 2 footers. Its monolithic factual assessment then exceeded the model's 180-second timeout. Assessment now uses bounded batches of at most eight requirements and eight claims, with the complete printed CV and confirmed evidence available to every batch. Every call remains metered and budget-reserved. The merger requires exact, unique coverage of each requested ID and produces no partial score when any batch fails. Existing final quote and factual-evidence validation remains in force. Timeout errors are identified explicitly, and retrying assessment retains the fitted wording. Regression tests cover unequal requirement/claim counts, complete context, usage accounting, later-batch failure and missing, duplicate or foreign claim IDs.

## Deployment handover

Render keeps the retiring instance briefly alongside the newly healthy instance. A live retry was claimed by the retiring instance and interrupted when it stopped. The task heartbeat then remained stale under the previous 20-minute recovery window. Recovery now uses five minutes of missed task heartbeats, aligned with the separate resource lease expiry; active long-running tasks continue renewing every 30 seconds and are not reclaimed. Attempt/owner fencing continues to prevent the interrupted worker writing after recovery. Node runs directly as the container entry process so shutdown signals reach the worker without package-runner indirection. Task-start logs now include the exact worker ID and commit. During live verification, inspect the instance that actually claims the CV task, not only the service health endpoint, and allow the deployment handover to finish before starting a new test.

## Assessment citation recovery

The recovered production task completed its model batches, then correctly rejected a supported claim attributed to another evidence block. Section claims now carry their required evidence ID explicitly, including skill claims, and each batch highlights those source records. A faulty attribution or quote receives one bounded correction request. If the model still cannot substantiate a finding, it becomes visibly uncertain/unknown with no unverified citations and no score credit; the CV wording is not changed. The existing strict final validator is unchanged and still rejects fabricated quotes or unsupported positive claims. Quote checks share the final validator's normalisation rule. Tests prove corrected attributions, bounded unresolved findings, exact source IDs, preservation of valid reviews, and zero points for unresolved citations. The successful first live batch used 11,842 output tokens in 123 seconds, so each eight-item batch now has 16,000 tokens and four minutes of headroom; work remains bounded and metered.
